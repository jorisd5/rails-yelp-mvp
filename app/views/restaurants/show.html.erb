<div class="d-flex flex-column text-center">

<h1>Reviews for: <%= @restaurant.name %></h1>
<% @reviews.each do |review| %>
  <div class="align-self-center p-2 border w-25 m-3">
    <p>Stars: <%= review.rating %></p>
    <p><%= review.content %></p>
  </div>
<% end %>

</div>
